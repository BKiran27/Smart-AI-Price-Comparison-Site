services:
  - type: web
    name: smart-buyer-ai
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
